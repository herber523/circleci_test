machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t helloworld .

test:
  override:
    - docker run -d -p 5000:5000 helloworld; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:5000

